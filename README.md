# protobuf-forensic

Schemaless forensic Protocol Buffers decoder — decode raw protobuf bytes with no
`.proto`, walk the wire format into a field tree, and apply forensic heuristics.

Scaffold in progress; see the full README once the decoder lands.
